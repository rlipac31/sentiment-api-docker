# Docker Compose Environment Variables
# IMPORTANTE: NO subir este archivo con credenciales reales a GitHub
# Usar variables de entorno del sistema o archivo .env separado

# Database Configuration (URL completa para Spring Boot)
DB_URL=jdbc:mysql://YOUR_MYSQL_HOST/YOUR_DATABASE_NAME
DB_USER=YOUR_USERNAME
DB_PASSWORD=YOUR_PASSWORD

# JWT Secret
JWT_SECRET=YOUR_SECRET_KEY

# Usage in docker-compose.yml:
# - SPRING_DATASOURCE_URL=jdbc:mysql://${DB_HOST}/${DB_NAME}
# - SPRING_DATASOURCE_USERNAME=${DB_USER}
# - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
# - PALABRA_SECRETA_JWT=${JWT_SECRET}
